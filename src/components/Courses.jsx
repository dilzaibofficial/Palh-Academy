import React from "react";

const Courses = () => {
  return <React.StrictMode>
    <div className="intro-page" id="coursespage">
    <h1>Courses</h1>
    </div>
  </React.StrictMode>;
};

export default Courses;
